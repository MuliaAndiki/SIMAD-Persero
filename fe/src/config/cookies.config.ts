/**
 * Re-export konfigurasi cookie dari `@/configs/cookies.config`.
 * Berada di `config/` agar kompatibel dengan import lama
 * (fe/src/api/client/client-http.ts).
 */
export * from '@/configs/cookies.config';
